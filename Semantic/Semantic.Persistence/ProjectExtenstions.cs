using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Project;

namespace Project
{
    public static class ProjectExtenstions
    {
        //public static string FullDbType(this ColumnType column)
        //{
        //    var rslt = column.DbType;
        //    if ( column.Size > 0 )
        //        rslt += "(" + column.Size;
        //    if(column.Scale > 0 )
        //        rslt += "," + column.Scale;
        //    if (column.Size > 0)
        //        rslt += ")";

        //    return rslt;
        //}
    }
}